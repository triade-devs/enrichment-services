name: Keep service alive

on:
  schedule:
    - cron: "*/13 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping enrichment-services
        run: curl -sf ${{ secrets.ENRICHMENT_URL }}/health || echo "serviço não respondeu"
